import {Component, OnInit, ViewEncapsulation} from '@angular/core';

@Component({
    selector: 'my-app',
    styleUrls: ['./src/style.scss'],
    encapsulation: ViewEncapsulation.None,
    template: `
        <div id="logo"></div>
    `
})
export class App {
    
    constructor() {}

    
}